version: 0.2
phases:
  install:
    commands:
      - n install 20
      - node --version
  pre_build:
    commands:

      - AWS_MAX_ATTEMPTS=10 aws cloudfront create-invalidation --distribution-id $CLOUDFRONT_ID --paths '/*'
